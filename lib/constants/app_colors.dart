import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.blue;
  static const secondary = Colors.grey;
  static const defaultBackground = Colors.white;
  static const defaultPenColor = Colors.blue;
  static const defaultTextColor = Colors.black;
}
